language: python
python:
  - "3.4"
  - "3.5"
# command to install dependencies
install: "pip install -r requirements.txt; pip install -r requirements-docs.txt; pip install -r requirements-dev.txt"
# command to run tests
script: python setup.py test
